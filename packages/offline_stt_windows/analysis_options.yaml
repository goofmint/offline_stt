analyzer:
  exclude:
    - build/**
    - windows/**
include: package:flutter_lints/flutter.yaml

# 公開APIのdocコメント欠落を静的解析で検出する(Issue #63)。
# 本リポジトリのdocコメントは design.md / requirements.md の節番号と
# 既知の制約を記録する役目を持つため、記述漏れをレビュー任せにしない。
linter:
  rules:
    - public_member_api_docs

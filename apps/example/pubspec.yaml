name: example
description: offline_stt の example app(雛形のみ、機能実装はIssue #32)。
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  file_picker: ^13.1.0
  flutter:
    sdk: flutter
  offline_stt: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
